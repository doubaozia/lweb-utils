import { hasClass, addClass, removeClass } from './classes';
import copyToClipBoard from './copyToClipBoard';
import { addEvent, removeEvent, checkPassiveSupport } from './event';
import loadImg from './loadImg';
import loadScript from './loadScript';
import ready from './ready';
import requestWithoutAjax from './requestWithoutAjax';
import scrollTo, { enableScroll } from './scrollTo';

export {
  hasClass,
  addClass,
  removeClass,
  copyToClipBoard,
  addEvent,
  removeEvent,
  checkPassiveSupport,
  loadImg,
  loadScript,
  ready,
  requestWithoutAjax,
  scrollTo,
  enableScroll,
};
