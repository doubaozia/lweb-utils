import {
  getCookie,
  setCookie,
  removeCookie,
} from './cookie';
import {
  getStorage,
  setStorage,
  getLocalStorage,
  getSessionStorage,
  setLocalStorage,
  setSessionStorage,
} from './local';

export {
  getCookie,
  setCookie,
  removeCookie,
  getStorage,
  setStorage,
  getLocalStorage,
  getSessionStorage,
  setLocalStorage,
  setSessionStorage,
};
